# E2E Tests

Deterministic integration tests for Situ's app-facing surfaces.

Use this project for CLI and local integration checks that should be stable in
local development and CI:

```bash
mise run test
```

Real LLM quality checks belong in `projects/evals/`.
