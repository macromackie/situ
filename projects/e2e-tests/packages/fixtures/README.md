# fixtures

Reusable fixture repositories and expected world data for deterministic tests
and e2e tests.

## tiny-autoresearch

`tinyAutoresearchFixture` materializes a compact repository for deterministic
Situ regressions. Its metadata preserves concise expected assertions for base
CLI inspection plus the current-repository project, task, and assigned-actor
notification flow.
