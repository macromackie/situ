# @situ/live

Append-only live presentation records used by the read-only local browser UI.

The package owns semantic UI records such as live signals, run-map nodes, run-map
edges, focus records, and node details. It does not own agent runtime state,
browser writes, report rendering, or arbitrary component authoring.
