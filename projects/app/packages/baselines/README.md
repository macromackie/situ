# baselines

Durable baseline reference records for autoresearch comparisons.

Baselines belong to a project, may optionally belong to a task, and hold the
human-readable description of what future experiment measurements should be
compared against. Metrics are recorded as ordinary measurements with
`baselineId`.
